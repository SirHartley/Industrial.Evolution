package indevo.exploration.meteor;

public class MeteorSwarmManager {
    //spawn in random system

    //system size is 52000x52000
    //and you can get that with StarsystemAPI.getMapGridWidthOverride and StarsystemAPI.getMapGridHeightOverride
    //You should test if those return any value if the size is is not overwritten, i.e if it returns null or something
    //In case it does, simply set the default value to 52000x52000, and if their not null, use those values

    //width 4000f, intensity 2 feels good (could probably even be wider)

}
